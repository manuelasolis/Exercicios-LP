#include <stdio.h>

int main(){
    int valor;
    float soma = 0;

    for(int i = 1; i <= 10; i++){
        printf("Digite um valor: ");
        scanf("%d", &valor);
        soma += valor;
    }

    printf("Media: %.2f\n", soma / 10);
}
