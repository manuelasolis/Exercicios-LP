include <stdio.h>

int main(){
    int valor, contador = 0;

    for(int i = 1; i <= 10; i++){
        printf("Digite um valor: ");
        scanf("%d", &valor);

        if(valor < 0){
            contador++;
        }
    }

    printf("Quantidade de valores negativos: %d\n", contador);
}
