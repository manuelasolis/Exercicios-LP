#include <stdio.h>

int main(){
    int N;

    printf("Digite o valor de N: ");
    scanf("%d", &N);

    while(N <= 0){
        printf("Digite um valor maior que 0: ");
        scanf("%d", &N);
    }

    for(int i = 1; i <= N; i++){
        printf("%d, ", i);
    }
}
