#include <stdio.h>

int main(){
    int valor, soma = 0;

    for(int i = 1; i <= 10; i++){
        printf("Digite valor: ");
        scanf("%d", &valor);

        if(valor < 40){
            soma += valor;
        }
    }

    printf("Soma dos valores: %d\n", soma);
}
